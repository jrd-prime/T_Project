using System;
using Game.UI._Base;

namespace Game.UI.Menu.State
{
    public sealed class MenuStateSettings : UISubStateBase
    {
        public MenuStateSettings(UISubStateBaseData data, Enum defaultSubState) : base(data, defaultSubState)
        {
        }

    }
}
